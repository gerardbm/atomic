#----------------------------------------------------------------
#   _____
#  |_   _| __ ___  _   ___  __
#    | || '_ ` _ \| | | \ \/ /
#    | || | | | | | |_| |>  <
#    |_||_| |_| |_|\__,_/_/\_\
#
#----------------------------------------------------------------
#  Theme   : Atomic
#  Version : 1.3.1
#  License : MIT
#  Author  : Gerard Bajona
#  URL     : https://github.com/gerardbm/atomic
#----------------------------------------------------------------

#----------------------------------------------------------------
# Theme Settings
#----------------------------------------------------------------

# Change the default prefix binding
unbind-key C-b
set -g prefix C-Space
bind-key Space send-prefix

# Use Zsh as default shell
# Disable it if you don't use Zsh
set-option -g default-shell /bin/zsh

# Limit the scrollback buffer to 10000 lines
set-option -g history-limit 10000

# Add UTF8 support
# set-window-option -g utf8 on
# - Option REMOVED since tmux version 2.2

# 256 colors support
set -g default-terminal "tmux-256color"
# - Why 'tmux-256color' instead of 'screen-256color'?
# - Because we want to enable italics into tmux
# - Compile this terminfo: 'tic tmux-256color.terminfo'.
# - For Konsole, compile this one, too: 'tic xterm-256color-italic.terminfo'.
# - Set 'TERM=tmux-256color' in gnome-terminal and xfce4-terminal.
# - Set 'TERM=xterm-256color-italic' in Konsole.

# Enable true color
set -ga terminal-overrides ",xterm-256color-italic:XT:Tc"
# - Option ADDED since tmux version 2.2
# - ':XT' flag restores 'automatic-rename' on tmux windows titles

# Enable xterm-keys
set-window-option -g xterm-keys on
set-window-option -g mode-keys vi

# Faster command sequences
set -sg escape-time 0

# Window titles
setw -g automatic-rename on
set-option -g set-titles on
set-option -g set-titles-string "#T"

# Start numbering from 1
set-option -g base-index 1
setw -g pane-base-index 1

# New window and clock mode
bind-key n new-window
bind-key c clock-mode

# Windows navigation (Vim-like)
unbind-key l
bind-key Space last-window
bind-key l next-window
bind-key h previous-window

# Split window into panes
unbind-key '"'
unbind-key %
bind-key . split-window -h
bind-key - split-window -v

# Panes
bind-key e list-panes
bind-key Enter kill-pane

# Panes navigation (Vim-like)
# bind-key -n M-h select-pane -L
# bind-key -n M-l select-pane -R
# bind-key -n M-k select-pane -U
# bind-key -n M-j select-pane -D

# Move between Tmux panes and Vim windows
# - It requires the corresponding configuration into Vim.
# - Check it at my .vimrc, section 9 («Multiple windows»).
# - URL: https://github.com/gerardbm/vimrc/blob/master/vimrc
# - Plugin required: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n M-h if-shell "$is_vim" "send-keys M-h"  "select-pane -L"
bind-key -n M-j if-shell "$is_vim" "send-keys M-j"  "select-pane -D"
bind-key -n M-k if-shell "$is_vim" "send-keys M-k"  "select-pane -U"
bind-key -n M-l if-shell "$is_vim" "send-keys M-l"  "select-pane -R"
bind-key -n M-\ if-shell "$is_vim" "send-keys M-\\" "select-pane -l"

# Swap panes (Vim-like)
bind-key k swap-pane -U
bind-key j swap-pane -D

# Resize panes (VIM-like)
bind-key -n C-M-h resize-pane -L 1
bind-key -n C-M-l resize-pane -R 1
bind-key -n C-M-k resize-pane -U 1
bind-key -n C-M-j resize-pane -D 1
bind-key -n S-F10 resize-pane -Z

# Copy mode (VIM-like)
bind-key v copy-mode
bind-key -t vi-copy Escape cancel
unbind-key p
bind-key p paste-buffer
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'V' select-line
bind-key -t vi-copy 'y' copy-selection
bind-key -t vi-copy 'r' rectangle-toggle

# Reload configuration
bind-key r source-file ~/.tmux.conf \; display "Tmux settings reloaded!"

# URLview
bind-key u capture-pane \; save-buffer /tmp/tmux-buffer \; new-window -n "urlview" 'BROWSER="qutebrowser" $SHELL -c "urlview < /tmp/tmux-buffer"'

#----------------------------------------------------------------
# Color Settings
# - The installation of Powerline fonts is highly recommended
# - URL: https://github.com/powerline/fonts
#----------------------------------------------------------------

# Message text
set-option -g message-fg colour0
set-option -g message-bg colour3

# Modes
setw -g mode-fg colour15
setw -g mode-bg colour8
setw -g clock-mode-colour colour6
setw -g clock-mode-style 24

# Panes border
set -g pane-border-bg colour0
set -g pane-border-fg colour7
set -g pane-active-border-bg colour0
set -g pane-active-border-fg colour6

# Panes number
set-option -g display-panes-active-colour colour2
set-option -g display-panes-colour colour3

# Statusbar
set -g status-fg colour6
set -g status-bg colour8
set -g status-left '#[fg=colour0,bg=colour6] #H #[default] s:#S w:#I p:#P  '
set -g status-right '  %Y-%m-%d #[fg=colour0,bg=colour6] %H:%M '
set -g status-attr dim

# Statusbar layout
set -g status-position bottom
set -g status-justify left
set -g status-right-length 50
set -g status-left-length 50

# Statusbar window tab
set-window-option -g window-status-fg colour8
set-window-option -g window-status-bg colour7
set-window-option -g window-status-format "#[fg=colour8,bg=colour7] #[fg=colour8]#I #[default]#[fg=colour8] #W #[fg=colour7,bg=colour8]"
set-window-option -g window-status-attr dim

# Statusbar window tab current
set-window-option -g window-status-current-fg colour0
set-window-option -g window-status-current-bg colour4
set-window-option -g window-status-current-format "#[fg=colour8,bg=colour4]#[default] #I #[fg=colour12]#[default] #W #[fg=colour4,bg=colour8]"
set-window-option -g window-status-current-attr dim

# Statusbar window tab activity
set-option -gw window-status-activity-fg colour2
set-option -gw window-status-activity-bg colour7
set-option -gw window-status-activity-attr dim

# Alerts
set-window-option -g monitor-activity on
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence on
set-option -g display-time 1500
